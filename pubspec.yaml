name: panda_map
description: Custom google map package
version: 0.0.1
homepage: https://github.com/dttrung-99931
publish_to: none

environment:
  sdk: ">=2.19.6 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  google_maps_flutter: ^2.3.0
  location: ^5.0.3
  dio: ^5.4.3+1 # for call google map API

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
